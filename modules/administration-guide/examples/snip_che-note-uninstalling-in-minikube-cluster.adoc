. When uninstalling {prod-short} from a Minikube cluster, remove the `dex` namespace and the `{prod-namespace}` namespace.
+
[subs="+attributes,quotes"]
----
$ {orch-cli} delete namespaces dex {prod-namespace}
----
